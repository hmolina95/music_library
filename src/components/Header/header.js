import React from 'react';
import Styles from './styles.css';

const Header = () => {
    return (
        <header>
            <h1>Biblioteca Musical</h1>
        </header>
    );
};

export default Header;
